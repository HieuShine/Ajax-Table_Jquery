@model IEnumerable<demoCuoiky_2.Models.Department>
    @foreach (var item in Model)
	{
		var url = "/emps/empbyid/" + item.deptid;
		<a href="@url">@item.deptname</a>
		<label>&nbsp;|&nbsp;</label>
	}
